language: c
compiler:
  - gcc
  - clang
branches:
  only:
      - master
script:
    - ./autogen.sh
    - ./configure --enable-ipv4 --enable-ipv6 --prefix=/opt/fping
    - make
    - sudo make install
    - sudo /opt/fping/sbin/fping 127.0.0.1
